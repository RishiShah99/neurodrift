# Copy to .env and fill in. Never commit .env.

# GCP — primary cloud for fleet GPUs and object storage
GOOGLE_APPLICATION_CREDENTIALS=
GCP_PROJECT=
GCS_BUCKET=neurodrift-data

# Dataset registry credentials (only for the fast-register cohorts)
OASIS_USERNAME=
OASIS_PASSWORD=
HCP_CONNECTOMEDB_USERNAME=
HCP_CONNECTOMEDB_PASSWORD=
CAMCAN_AGREEMENT_ID=

# Deferred to release day — uncomment when needed
# HF_TOKEN=
# MODAL_TOKEN_ID=
# MODAL_TOKEN_SECRET=
